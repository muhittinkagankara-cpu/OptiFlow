# Uretim Sureç Simulasyon Motoru — çalisma zamani bagimliliklari
#
# Yalnizca sunucunun calismasi icin gerekenler burada tutulur. Test araçlari
# `requirements-dev.txt` dosyasindadir; yayina alma ortaminda pytest ve httpx
# kurmak gereksiz yere derleme suresini ve kapsayici boyutunu buyutur.

pydantic>=2.6,<3.0
fastapi>=0.110
uvicorn[standard]>=0.29

# Kalici depolama. DATABASE_URL tanimli degilse ikisi de kullanilmaz ve
# sonuclar bellekte tutulur; yine de kurulu olmalari gerekir cunku depolama
# katmani ice aktarilirken SQLAlchemy'ye ihtiyac duyar.
sqlalchemy>=2.0
psycopg2-binary>=2.9
alembic>=1.13

# Kimlik dogrulama: Supabase erisim token'larinin (JWT) dogrulanmasi.
#
# `[crypto]` ZORUNLUDUR, istege bagli degil: Supabase'in 1 Mayis 2025'ten
# sonra acilan projelerdeki varsayilani asimetrik imzalamadir (RS256/ES256) ve
# PyJWT bu algoritmalari yalnizca `cryptography` kuruluysa kaydeder. Sade
# `pyjwt` ile dagitim yapilirsa her giris denemesi calisma zamaninda
# "Algorithm not supported" ile duser.
pyjwt[crypto]>=2.8,<3.0

# Runtime koprusu: fabrika cihazlariyla konusan surucular (SALES-9).
#
# Ikisi de **tembel** ice aktarilir: kurulu degillerse uygulama yine acilir ve
# ilgili protokol arayuzde "Dogrulanmadi" gorunur. Yine de calisma zamani
# bagimliligi olarak listelenirler, cunku kurulu olmadiklarinda OPC UA ve MQTT
# baglantilari hic denenemez -- yayindaki bir kurulumda bu, ozelligin sessizce
# yok olmasi demek olurdu.
asyncua>=1.1,<3.0
aiomqtt>=2.0,<3.0
